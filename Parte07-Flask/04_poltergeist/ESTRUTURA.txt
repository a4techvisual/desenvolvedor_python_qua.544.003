Git Poltergeist v2.0
├── main.py / main.pyw       -> programa
├── CRIAR_EXE.vbs            -> cria o EXE sem mostrar CMD
├── executar.vbs             -> abre o EXE sem mostrar CMD
├── build_exe.bat             -> alternativa de compilação (não usar para abrir o programa)
├── Git Poltergeist v2.0.spec
├── image\                   -> ícone/logo/GIF
├── index\                   -> arquivos da interface original
└── font\                    -> fontes/estilos

O arquivo executar.bat foi removido para impedir o fluxo de inicialização via CMD.
